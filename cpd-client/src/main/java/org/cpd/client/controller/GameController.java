package org.cpd.client.controller;

public class GameController {
    private RequestController controller;

    public GameController(RequestController controller) {
        this.controller = controller;
    }
}
